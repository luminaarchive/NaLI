# Correction log (F6.1)

Each correction is one JSON file matching the Correction interface in lib/corrections.ts:

  { "id", "artikelSlug", "artikelTitle", "tanggalDiterima", "tanggalDiperbaiki",
    "klaimLama", "klaimBaru", "alasan", "sumberKoreksi" }

No corrections issued yet. The /koreksi page shows an honest empty state until the first one lands.
